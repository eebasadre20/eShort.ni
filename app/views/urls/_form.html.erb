<%= form_with(model: url, local: true) do |form| %>
  <% if url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(url.errors.count, "error") %> prohibited this url from being saved:</h2>

      <ul>
      <% url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url_code %>
    <%= form.text_field :url_code, id: :url_url_code %>
  </div>

  <div class="field">
    <%= form.label :original_url %>
    <%= form.text_field :original_url, id: :url_original_url %>
  </div>

  <div class="field">
    <%= form.label :generated_hash %>
    <%= form.number_field :generated_hash, id: :url_generated_hash %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
